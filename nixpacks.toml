[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev --no-fund --no-audit"]

[phases.build]
cmds = ["echo \"Using pre-built dist\""]

[start]
cmd = "node dist/index.js"
